# conda environment requirements
channels:
- conda-forge
dependencies:
- maite >= 0.6.0
# test
- pytest  # TODO: sort out dev/test deps properly
- pytest-cov
# linting and formatting
- pre-commit
- pyright
